// Zig로 서보 제어 예시 (개념적 코드)
const pwm = @import("hardware").pwm;
pub fn servoWrite(pin: u8, angle: u8) void {
    const pulse_us = 500 + angle * (2000 / 180); // 500~2500μs 변환
    pwm.setPulseWidth(pin, pulse_us);
}
